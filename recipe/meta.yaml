{% set name = "spatial-image" %}
{% set version = "1.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/spatial_image-{{ version }}.tar.gz
  sha256: ec6c043bf985411f882b9be6c6f061c8b0bc8723a9899e76e149def2035471dd

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - flit-core
    - pip
    - python {{ python_min }}
  run:
    - numpy
    - python >={{ python_min }}
    - xarray
    - xarray-dataclasses >=1.9.1

test:
  imports:
    - spatial_image
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/spatial-image/
  summary: A multi-dimensional spatial image data structure for scientific Python.
  license: MIT
  license_file: LICENSE.txt
  dev_url: https://github.com/spatial-image/spatial-image

extra:
  recipe-maintainers:
    - goanpeca
    - giovp
    - LucaMarconato
